Heroku buildpack: Poppler (DO NOT USE)
======================================

Instead of using this buildpack, use [heroku-buildpack-apt](https://elements.heroku.com/buildpacks/heroku/heroku-buildpack-apt) and specify `poppler-utils` in the Aptfile. 
